// import { randomInt } from './lib/random.js'
// import { webcrypto as crypto } from 'crypto'
// import { add } from './lib/bitwise.js'

// const mod = (n: number, m: number) => (n % m + m) % m

// let i, len = 100

// crypto.getRandomValues(new Uint8Array)

// for (i = 0; i < len; i++) {
//   const p = BigInt(37)
//   const n = randomInt(0, p)
//   const e = Math.pow(n, p-1)

//   console.log(n, e, mod(e - 1, p))
// }

console.log(typeof(150n))
